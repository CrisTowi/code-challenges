# Per-challenge unit architecture

Each challenge lives in its own folder containing both the algorithm and its visualization. The repo is a monorepo of challenges, not a separation of "solutions" and "visualizations". A future reader who finds a challenge folder can read the algorithm and its scene side by side.